#summary OpenPGM 5 : C Reference : Environment : pgm_init()
#labels Phase-Implementation
#sidebar TOC5CReferenceEnvironment
_Function_
=== Declaration ===
<pre>
#include <pgm/pgm.h>

bool *pgm_init* ([OpenPgm5CReferencePgmErrorT pgm_error_t]`*``*`     error);
</pre>

=== Purpose ===
Create and start OpenPGM internal machinery.

=== Remarks ===
This call creates the internal machinery that OpenPGM requires for its operation:

  * Messaging support.
  * Thread synchronisation.
  * Memory management.
  * Pseudo random number generator.
  * Timer support.
  * PGM protocol number.
 
Until the first call to <tt>pgm_init()</tt>, all events, queues, and transports are unusable.

<tt>pgm_init()</tt> implements reference counting, every call needs to be matched with a call to <tt>[OpenPgm5CReferencePgmShutdown pgm_shutdown()]</tt>. If you want to make sure that the PGM engine is initialized without references, you can do this:

{{{
 if (!pgm_supported ()) pgm_init (NULL);
}}}

On Microsoft Windows platforms <tt>pgm_init()</tt> will request 1ms minimum resolution timers via the <tt>[http://msdn.microsoft.com/en-us/library/dd757624(v=vs.85).aspx timeBeginPeriod()]</tt> function.  The resolution period is relinquished on shutdown of the PGM engine via <tt>[OpenPgm5CReferencePgmShutdown pgm_shutdown()]</tt>.

=== Parameters ===

<table border="1" cellpadding="5" cellspacing="0">
<tr>
<th>Parameter</th>
<th>Description</th>
</tr>
<tr>
<td><tt>err</tt></td>
<td>a return location for a <tt>[OpenPgm5CReferencePgmErrorT pgm_error_t]</tt>, or <tt>NULL</tt>.</td>
</tr>
</table>


=== Return Value ===
On success, returns <tt>true</tt>.  On failure, returns <tt>false</tt>.  If <tt>err</tt> is provided it may be set detailing any Winsock or timing device initialization issue.


=== Example ===
{{{
 pgm_error_t *err = NULL;
 if (!pgm_init (&err)) {
   fprintf (stderr, "PGM Engine initialization error %s\n", (err && err->message) ? err->message : "(null)");
   pgm_error_free (err);
   return EXIT_FAILURE;
 }
}}}

=== See Also ===
  * <tt>[OpenPgm5CReferencePgmSupported pgm_supported()]</tt><br>
  * <tt>[OpenPgm5CReferencePgmShutdown pgm_shutdown()]</tt><br>
  * <tt>[http://msdn.microsoft.com/en-us/library/dd757624(v=vs.85).aspx timeBeginPeriod()]</tt> in MSDN Library.<br>
  * [OpenPgm5CReferenceEnvironment Environment] in OpenPGM C Reference.<br>
  * [OpenPgm5CReferenceErrorHandling Error Handling] in OpenPGM C Reference.
